<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sofascore.pokedex">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PokedexApplication">
        <activity android:name=".ui.activity.AboutActivity"></activity>
        <activity
            android:name=".ui.activity.TypeDetailActivity"
            android:label="@string/title_activity_type_detail"
            android:theme="@style/Theme.PokedexApplication.NoActionBar" />
        <activity
            android:name=".ui.activity.DetailPokemonActivity"
            android:label="@string/title_activity_detail_pokemon"
            android:theme="@style/Theme.PokedexApplication.NoActionBar" />
        <activity
            android:name=".ui.activity.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>